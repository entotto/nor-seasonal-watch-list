{% extends 'base.html.twig' %}

{% block title %}Discord Channel{% endblock %}

{% block body %}
    <h1>DiscordChannel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discord_channel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ discord_channel.name }}</td>
            </tr>
            <tr>
                <th>Hidden</th>
                <td>{{ discord_channel.hidden ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_discord_channel_index') }}">back to list</a>

    <a href="{{ path('admin_discord_channel_edit', {'id': discord_channel.id}) }}">edit</a>

    {{ include('discord_channel/_delete_form.html.twig') }}
{% endblock %}
