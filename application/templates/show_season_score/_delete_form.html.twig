<form method="post" action="{{ path('admin_show_season_score_delete', {'id': show_season_score.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ show_season_score.id) }}">
    <button class="btn">Delete</button>
</form>
