{% extends 'base.html.twig' %}

{% block title %}Election Vote{% endblock %}

{% block body %}
    <h1>ElectionVote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ election_vote.id }}</td>
            </tr>
            <tr>
                <th>Chosen</th>
                <td>{{ election_vote.chosen ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_election_vote_index') }}">back to list</a>

    <a href="{{ path('admin_election_vote_edit', {'id': election_vote.id}) }}">edit</a>

    {{ include('election_vote/_delete_form.html.twig') }}
{% endblock %}
